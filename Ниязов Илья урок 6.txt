1) 
#!/bin/bash 
if [ $1 == "-w" ]; then
        sed -i '/^$/d' $2 && sed -i -e 's/\(.*\)/\U\1/' $2
        cat $2
elif [ $1 == "-h" ]; then
        echo "Для того что бы вывести на экран напишите ./test.sh 'имя файла',
        если надо переписать этот файл то напишите ./test.sh -w 'имя файла'"
else
        sed '/^$/d' $1 && sed -e 's/\(.*\)/\U\1/' $1
fi
